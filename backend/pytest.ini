[pytest]

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    --tb=short
    -p no:warnings

asyncio_mode = auto

markers =
    slow: tests involving real network or DB I/O
    integration: tests requiring a live PostgreSQL instance
